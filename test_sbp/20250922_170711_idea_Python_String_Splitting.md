---
title: Python String Splitting
link: https://realpython.com/courses/python-string-splitting/
summary: 'The article provides an overview of two popular Python functions for string manipulation: `.split()` and `re.split()`. The `.split()` function splits a string into a list of substrings based on the specified delimiter. It can be used with or without whitespace, delimiters like commas, or multiline text.

On the other hand, `re.split()` is more versatile as it supports regular expressions to split strings according to patterns. This makes it easier to handle complex patterns and edge cases.

The article also provides examples of using `.split()` and `re.split()` with different types of input data, such as a single string, a list, or a multi-line string.

In summary, the article highlights the utility and flexibility of Python's string manipulation functions in handling various types of text data.'
tags:
- computer-vision
- machine-learning
- programming
- ai
- software
content_hash: d16fe60758a21ecd8c7d0f69e9444340ce24a1a8061fef952fd57418e27da826
feed_title: Real Python
feed_url: https://realpython.com/atom.xml
date_processed: '2025-09-22T17:07:11.011853'
category: 24-computing
---

Master Python string splitting with .split() and re.split() to handle whitespace, delimiters and multiline text.