views:
  - type: table
    name: Table
    filters:
      and:
        - "!file.tags.isEmpty()"
        - file.tags.containsAny("ai", "arXiv")
    order:
      - file.name
      - tags
    sort:
      - property: tags
        direction: ASC
